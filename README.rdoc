== Affiliate Sites 

ruby 2.0.0
rails 4.2

Ignore the migrations. in db folder there is a schema dump that you can use instead. schema name 'development'

make mappings in your host file for abc.com and def.com to localhost.

Visiting these domain against the app, will POC the semi-dynamic layouts setup of the app.

